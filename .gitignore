_build/
/vinifera-collector
bin/
node_modules/
ui/dist/
ui/node_modules/
# Built SPA assets are embedded at Docker build time; only the placeholder
# index.html is tracked so `go:embed all:web/dist` compiles in local dev.
extension/viniferaui/web/dist/assets/
*.db
*.db-wal
*.db-shm
coverage.out
# Local Go module/build caches (used when building outside Docker). Never commit.
.gopath/
.gocache/
go.mod.test
*.log
.env
.env.*
!.env.example
.DS_Store
