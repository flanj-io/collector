# Tier: eval — sqlite on an emptyDir. Nothing survives the pod.
specToken:
  value: ci-eval-token-not-a-real-secret
integration:
  id: acme-payments
  consumerDisplayName: Acme Consumer Ltd
collector:
  replicas: 2
store:
  backend: sqlite
  persistence:
    enabled: false
